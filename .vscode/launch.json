{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch popcorn with test",
      "preLaunchTask": "make build",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "console": "integratedTerminal",
      "args": ["./example.pop"]
    }
  ]
}